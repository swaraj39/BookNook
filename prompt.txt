Act as a senior professional UI/UX developer. You are to only touch frontend/ 
do not touch any other file apart from that folder. 

1.responsive for mobile as well as very large screens
2.sidebar make to top navigation
3.user profile shoubld be visible to right top just like in strandard format
4.theme should be in around #FDFBF7 and #667FFF
5.also create a simple home page as well ( take inspiration from this react code )
    <react code>
    import React
     from "react";

const HeroSection = () => {
  return (
    <section className="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 via-slate-100 to-slate-200 px-6">
      <div className="max-w-5xl text-center">
        {/* Badge */}
        <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white shadow-sm border border-slate-200 mb-8">
          <span className="text-blue-500">✦</span>
          <span className="text-sm text-slate-700">
            A reading community
          </span>
        </div>

        {/* Heading */}
        <h1 className="text-6xl md:text-8xl font-bold tracking-tight leading-none text-slate-900">
          Borrow a book.{" "}
          <span className="text-blue-500">Pass it on.</span>
        </h1>

        {/* Description */}
        <p className="mt-8 max-w-3xl mx-auto text-xl text-slate-600 leading-relaxed">
          Book Nook is a shared shelf for our team. List a book you'd
          lend, borrow one you've been meaning to read, and swap stories
          along the way.
        </p>

        {/* Buttons */}
        <div className="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
          <button className="px-8 py-4 rounded-xl bg-blue-500 text-white font-semibold shadow-md hover:bg-blue-600 transition">
            Browse the shelf
          </button>

          <button className="px-8 py-4 rounded-xl border border-slate-300 bg-white text-slate-900 font-semibold shadow-sm hover:bg-slate-50 transition">
            Lend a book
          </button>
        </div>
      </div>
    </section>
  );
};

export default HeroSection;
</react code>


	1.Books on the shelf (total number of books)
	2.Available to borrow (books that are not borrowed)
	show these two  on home page

 at the end of home page add "How it works". Take inspiration from here 
    <react code>
    import React from "react";

const steps = [
  {
    number: "01",
    title: "List a book",
    description:
      "Add a book you own and are happy to lend. Takes 30 seconds.",
  },
  {
    number: "02",
    title: "Request to borrow",
    description:
      "Find a title you love and send a quick request to the owner.",
  },
  {
    number: "03",
    title: "Read & return",
    description:
      "Enjoy the read, then return it so the next reader can dive in.",
  },
];

const HowItWorks = () => {
  return (
    <section className="py-24 bg-slate-100 px-6">
      <div className="max-w-7xl mx-auto">
        {/* Heading */}
        <h2 className="text-5xl font-bold text-center text-slate-900 mb-16">
          How it works
        </h2>

        {/* Cards */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {steps.map((step) => (
            <div
              key={step.number}
              className="bg-white rounded-3xl border border-slate-200 p-8 shadow-sm"
            >
              <div className="text-3xl font-semibold text-blue-500 mb-6">
                {step.number}
              </div>

              <h3 className="text-3xl font-bold text-slate-900 mb-4">
                {step.title}
              </h3>

              <p className="text-lg text-slate-600 leading-relaxed">
                {step.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

export default HowItWorks;
</react code>
8. Change the name catelog to browse
9. Add pagination to the now browse page , by default the limit is 20
10. change my book to my shelf
11. make subtl UI improvements for better user experience